<div className="min-h-screen bg-black">
            {/* Top Navbar */}
            <nav className="bg-[#171717] border-b border-white/20 sticky top-0 z-50">
                <div className="max-w-[1400px] mx-auto px-4 sm:px-6 py-4">
                    <div className="flex items-center justify-between">
                        {/* Logo */}
                        <Link to="/" className="flex items-center gap-2">
                            <h1 className="text-xl sm:text-2xl font-black text-white"
                                style={{ fontFamily: 'outfit, outfit Fallback' }}>
                                LevelUp<span className="text-blue-500">.dev</span>
                            </h1>
                        </Link>

                        {/* Profile */}
                        <Link to="/profile">
                            <div className="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white font-bold text-sm cursor-pointer hover:scale-105 transition-transform">
                                MS
                            </div>
                        </Link>
                    </div>
                </div>
            </nav>

            {/* Main Content */}
            <div className="max-w-[1400px] mx-auto px-4 sm:px-6 py-6 sm:py-8">

                {/* Course Header Card */}
                <div className="bg-[#171717] rounded-2xl p-6 mb-6 border border-white/10">
                    {/* Back Button & Course Name */}
                    <Link to="/dashboard" className="inline-flex items-center gap-2 text-gray-400 hover:text-white mb-4 transition-colors">
                        <ArrowLeft className="w-5 h-5" />
                        <span className="text-sm font-medium">Back to Dashboard</span>
                    </Link>

                    <h1 className="text-2xl sm:text-3xl md:text-4xl font-black text-white mb-6"
                        style={{ fontFamily: 'outfit, outfit Fallback' }}>
                        {courseData.name}
                    </h1>

                    {/* Completion Stats */}
                    <div className="mb-4">
                        <div className="flex items-center justify-between mb-2">
                            <span className="text-2xl font-bold text-blue-500">
                                {courseData.completion}% Completed
                            </span>
                        </div>

                        {/* Progress Bar */}
                        <div className="w-full bg-gray-800 rounded-full h-2 mb-6">
                            <div
                                className="bg-blue-500 h-full rounded-full transition-all duration-500"
                                style={{ width: `${courseData.completion}%` }}
                            ></div>
                        </div>

                        {/* Stats Grid */}
                        <div className="grid grid-cols-3 gap-4">
                            <div>
                                <p className="text-sm text-gray-400 mb-1">Modules</p>
                                <p className="text-lg sm:text-xl font-bold text-white">
                                    {courseData.modules.completed}/{courseData.modules.total}
                                </p>
                            </div>
                            <div>
                                <p className="text-sm text-gray-400 mb-1">Sub-Modules</p>
                                <p className="text-lg sm:text-xl font-bold text-white">
                                    {courseData.subModules.completed}/{courseData.subModules.total}
                                </p>
                            </div>
                            <div>
                                <p className="text-sm text-gray-400 mb-1">Score</p>
                                <p className="text-lg sm:text-xl font-bold text-white">
                                    {courseData.score}/{(courseData.totalScore / 1000).toFixed(1)}K
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                {/* 2-Column Layout */}
                <div className="grid grid-cols-1 lg:grid-cols-12 gap-6">

                    {/* Left Section - Modules (65-70%) */}
                    <div className="lg:col-span-8">
                        <div className="bg-[#171717] rounded-2xl border border-white/10 overflow-hidden">

                            {/* Tabs */}
                            <div className="border-b border-white/10">
                                <div className="flex">
                                    <button
                                        onClick={() => setActiveTab('modules')}
                                        className={`px-6 py-4 text-sm font-semibold transition-colors relative ${activeTab === 'modules'
                                                ? 'text-white'
                                                : 'text-gray-400 hover:text-gray-300'
                                            }`}
                                    >
                                        All Modules
                                        {activeTab === 'modules' && (
                                            <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-blue-500"></div>
                                        )}
                                    </button>
                                    <button
                                        onClick={() => setActiveTab('announcement')}
                                        className={`px-6 py-4 text-sm font-semibold transition-colors relative ${activeTab === 'announcement'
                                                ? 'text-white'
                                                : 'text-gray-400 hover:text-gray-300'
                                            }`}
                                    >
                                        Announcement
                                        {activeTab === 'announcement' && (
                                            <div className="absolute bottom-0 left-0 right-0 h-0.5 bg-blue-500"></div>
                                        )}
                                    </button>
                                </div>
                            </div>

                            {/* Modules List */}
                            <div className="p-4 sm:p-6">
                                {activeTab === 'modules' ? (
                                    <div className="space-y-3">
                                        {modules.map((module) => (
                                            <div key={module.id} className="bg-[#232323] rounded-xl overflow-hidden border border-white/5 hover:border-white/10 transition-all">
                                                {/* Module Header */}
                                                <button
                                                    onClick={() => toggleModule(module.id)}
                                                    className="w-full px-4 sm:px-6 py-4 flex items-center justify-between hover:bg-[#2a2a2a] transition-colors"
                                                >
                                                    <span className="text-base sm:text-lg font-semibold text-white">
                                                        {module.title}
                                                    </span>
                                                    {expandedModules[module.id] ? (
                                                        <ChevronUp className="w-5 h-5 text-gray-400" />
                                                    ) : (
                                                        <ChevronDown className="w-5 h-5 text-gray-400" />
                                                    )}
                                                </button>

                                                {/* Lessons List */}
                                                {expandedModules[module.id] && (
                                                    <div className="px-4 sm:px-6 pb-4 space-y-2">
                                                        {module.lessons.map((lesson) => (
                                                            <div
                                                                key={lesson.id}
                                                                className="flex items-center gap-3 py-3 px-4 rounded-lg bg-black hover:bg-[#2a2a2a] transition-colors cursor-pointer"
                                                            >
                                                                {getStatusIcon(lesson.status)}
                                                                <span className={`text-sm ${lesson.status === 'locked'
                                                                        ? 'text-gray-500'
                                                                        : 'text-gray-300'
                                                                    }`}>
                                                                    {lesson.title}
                                                                </span>
                                                            </div>
                                                        ))}
                                                    </div>
                                                )}
                                            </div>
                                        ))}
                                    </div>
                                ) : (
                                    <div className="text-center py-12">
                                        <p className="text-gray-400">No announcements yet</p>
                                    </div>
                                )}
                            </div>
                        </div>
                    </div>

                    {/* Right Section - Leaderboard (30-35%) */}
                    <div className="lg:col-span-4">
                        <div className="bg-[#171717] rounded-2xl p-6 border border-white/10 sticky top-24">
                            <h2 className="text-xl sm:text-2xl font-black text-white mb-6"
                                style={{ fontFamily: 'outfit, outfit Fallback' }}>
                                Leaderboard
                            </h2>

                            {/* Top 3 Podium */}
                            <div className="flex items-end justify-center gap-4 mb-8">
                                {/* 2nd Place */}
                                <div className="flex flex-col items-center">
                                    <div className="w-12 h-12 rounded-full bg-gradient-to-br from-gray-400 to-gray-600 flex items-center justify-center text-white font-bold mb-2">
                                        2
                                    </div>
                                    <div className="w-16 bg-gray-700 rounded-t-lg" style={{ height: '60px' }}></div>
                                </div>

                                {/* 1st Place */}
                                <div className="flex flex-col items-center">
                                    <div className="w-14 h-14 rounded-full bg-gradient-to-br from-yellow-400 to-yellow-600 flex items-center justify-center text-white font-bold mb-2">
                                        1
                                    </div>
                                    <div className="w-16 bg-yellow-600 rounded-t-lg" style={{ height: '80px' }}></div>
                                </div>

                                {/* 3rd Place */}
                                <div className="flex flex-col items-center">
                                    <div className="w-12 h-12 rounded-full bg-gradient-to-br from-orange-400 to-orange-600 flex items-center justify-center text-white font-bold mb-2">
                                        3
                                    </div>
                                    <div className="w-16 bg-orange-600 rounded-t-lg" style={{ height: '40px' }}></div>
                                </div>
                            </div>

                            {/* Leaderboard Table */}
                            <div className="space-y-2">
                                {/* Table Header */}
                                <div className="grid grid-cols-3 gap-2 px-4 py-2 text-xs font-semibold text-gray-400 border-b border-white/10">
                                    <span>Name</span>
                                    <span className="text-center">Rank</span>
                                    <span className="text-right">Points</span>
                                </div>

                                {/* Table Rows */}
                                {leaderboard.map((user, index) => (
                                    <div
                                        key={index}
                                        className={`grid grid-cols-3 gap-2 px-4 py-3 rounded-lg transition-colors ${user.isCurrentUser
                                                ? 'bg-blue-500/10 border border-blue-500/30'
                                                : 'bg-[#232323] hover:bg-[#2a2a2a]'
                                            }`}
                                    >
                                        <span className={`text-sm truncate ${user.isCurrentUser ? 'text-blue-400 font-semibold' : 'text-gray-300'
                                            }`}>
                                            {user.name}
                                        </span>
                                        <span className={`text-sm text-center ${user.isCurrentUser ? 'text-blue-400 font-semibold' : 'text-gray-300'
                                            }`}>
                                            {user.rank}
                                        </span>
                                        <span className={`text-sm text-right font-bold ${user.isCurrentUser ? 'text-blue-400' : 'text-white'
                                            }`}>
                                            {user.points}
                                        </span>
                                    </div>
                                ))}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>